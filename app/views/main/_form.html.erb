<dl>
  <% @model.attributes.each do |attribute| %>
    <% name = attribute[:name] %>

    <dt><%= name %></dt>
    <dd>

      <% if attribute[:key?] %>
        <%= @object.send( name ) || '&nbsp' %>

      <% else %>
        <% case attribute[:type] %>
        <% when :string, :integer %>
          <input type="text" name="model[<%= name %>]" value="<%= @object.send( name ) %>" />
        <% when :text %>
          <textarea name="model[<%= name %>]"><%= @object.send( name ) %></textarea>
        <% when :datetime, :date %>
          <input type="text" name="model[<%= name %>]" value="<%= @object.send( name ) %>" />
        <% when :bool %>
          Yes <input type="radio" name="model[<%= name %>]" value="true" checked="<%= @object.send( name ) && 'checked' %>" />
          No <input type="radio" name="model[<%= name %>]" value="false" checked="<%= @object.send( name ) || 'checked' %>" />
        <% else %>
          <%= "Don't know how to edit #{ name }, type: #{ attribute[:type] }" %>
        <% end %>
      <% end %>
    </dd>
  <% end %>

  <% for bt in @model.belongs_to_associations %>
    <dt><%= bt[:name] %></dt>
    <dd>
      <%#= partial( 'belongs_to', :association => bt ) %>
    </dd>
  <% end %>

  <% for hm in @model.has_many_associations %>
    <%#= partial( 'has_many_edit', :model => model, :association => hm ) %>
  <% end %>
</dl>